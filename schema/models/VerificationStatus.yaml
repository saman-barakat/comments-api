#   VerificationStatus:
      type: object
      properties:
        isVerified:
          type: boolean
        verificationTimestamp:
          type: string
          format: date-time
        verificationMethod:
          type: string
      xml:
        name: VerificationStatus